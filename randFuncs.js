const journey = (destination, flightDuration, home) => {
    return `This person started in London. They then travelled to ${destination}.  It took ${flightDuration} hours to get there.`;
}

//Create function that capitalises the first letter of every word in a string
const pascalCase = (input) => {

};

module.exports = { journey, pascalCase };